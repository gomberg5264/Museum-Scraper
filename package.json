{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "npx --yes pnpm@latest install --no-frozen-lockfile",
    "build": "npx pnpm --filter @workspace/api-server run build && npx pnpm --filter @workspace/nyc-museums run build",
    "start": "npm run build && PORT=3000 PUBLIC_DIR=artifacts/nyc-museums/dist/public node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "pnpm run typecheck:libs",
    "typecheck": "pnpm run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
